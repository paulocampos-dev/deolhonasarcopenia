# Copy to .env on the VPS (never commit the real .env)
PORT=4100
SITE_ROOT=/app/site
ADMIN_USERNAME=admin
# Generate with: node -e "console.log(require('bcryptjs').hashSync(process.argv[1], 12))" -- 'your-password'
ADMIN_PASSWORD_HASH=
SESSION_SECRET=
SITE_URL=https://deolhonasarcopenia.com.br
